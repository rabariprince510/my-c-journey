#include<stdio.h>
#include<conio.h>

main()
{
  int l;

  clrscr();
  printf("enter length: ");
  scanf("%d",&l);

  printf("%d * %d = %d",l,l,l*l);

  printf("\narea of square: %d",l*l);
  getch();
 }
